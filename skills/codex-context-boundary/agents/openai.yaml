interface:
  display_name: "Codex Context Boundary"
  short_description: "Repair routed context and subagent decryption"
  default_prompt: "Use $codex-context-boundary to check and repair opencodex routing, reboot protection, encrypted context, and V2 subagent delivery."

policy:
  allow_implicit_invocation: true
